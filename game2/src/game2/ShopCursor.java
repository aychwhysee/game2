package game2;

public class ShopCursor {
    
    // Just be a blue filled circle. down and up moves it back and forth
    // between selections
    
    // have a checker to see which ShopItem it is lined up with so that when
    // player selects to purchase that item it'll actually purchase it (if
    // player has enough money)
    
    // maybe instead of having one item getting more and more expensive as you
    // buy it (diminished rewards), I could just have a long list of items you 
    // can buy, but each succeeding item is more pricey than the other.
    
}
